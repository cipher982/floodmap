# ---------------------------------------------------------------------------
# Copy this file to `.env` and replace the placeholder values with your own.
# These variables are read by the application at runtime.
# ---------------------------------------------------------------------------

# Google Maps JavaScript API key (required for map rendering)
GMAP_API_KEY=REPLACE_ME

# IP2Location API key (used for IP-based geolocation fallback)
IP2LOC_API_KEY=REPLACE_ME

# USGS EarthExplorer credentials — only needed if you use the download scripts
USGS_USER=REPLACE_ME
USGS_TOKEN=REPLACE_ME

# ---------------------------------------------------------------------------
# Local data paths — can be absolute or relative. The Docker image provides
# defaults, but you can override them here.
# ---------------------------------------------------------------------------
INPUT_DIR=./data/input          # Directory containing raw GeoTIFF elevation files
PROCESSED_DIR=./data/processed  # Output directory for generated map tiles
COLOR_RAMP=./scripts/color_ramp.txt  # Path to the color-ramp text file
CACHE_DIR=./cache               # DiskCache storage (optional)

# ---------------------------------------------------------------------------
# GDAL / PROJ overrides (optional). Uncomment and set only if you need custom
# library paths on the host system.
# ---------------------------------------------------------------------------
# GDAL_PAM_ENABLED=NO
# CPL_DEBUG=OFF
# GDAL_LIBRARY_PATH=/usr/lib/libgdal.so
# PROJ_LIB=/usr/share/proj

# ---------------------------------------------------------------------------
# Rate limiting & external services
# ---------------------------------------------------------------------------
MAX_TILES_PER_SECOND=30          # Max tile requests per IP per second
REDIS_URL=redis://localhost:6379 # Optional Redis instance for distributed rate-limiting